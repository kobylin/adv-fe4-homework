<div class="movie">
    <div class="movie-image">
        <a href="#">
                <span class="play">
                    <span class="name">{{title}}</span>
                </span>
            <img src="{{imgUrl}}" alt="{{title}}"/>
        </a>
    </div>

    <div class="rating">
        <p>RATING</p>

        <div class="stars">
            <div class="stars-in stars-{{adjust_stars stars}}"></div>
        </div>
        <span class="comments">{{votes}}</span>
    </div>
</div>
